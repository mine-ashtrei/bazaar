import { Box, Button, Stack } from "@mui/material";
import CreateListingButton from "../../common/buttons/createListingButton";

const SupplierHeader = () => {
  return (
    <Box>
      <CreateListingButton />
    </Box>
  );
};

export default SupplierHeader;
